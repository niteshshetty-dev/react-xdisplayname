Application Requirements:
Your application must look as follows upon the initial render:

After entering first and last name, and clicking on the submit button, you must get the full name displayed as follows:

You must make the input fields such that the user is unable to submit without filling in both the fields.

NOTE:

You must make this form using the form element.
You must create the button using button element.
You must use the input fields using the input element.
